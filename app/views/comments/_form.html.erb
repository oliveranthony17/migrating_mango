<%= simple_form_for [topic.country, topic, comment] do |f| %>
  <%= f.input :title, label: "Title" %>
  <%= f.input :content, label: "Content" %>
  <%= f.button :submit, "Submit", class: "btn btn-primary" %>
<% end %>
